name: Docker Image fly.io Updated Deploy.

on:
  workflow_dispatch:
    inputs:
      login_email:
        required: true
        type: string
      login_password:
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
         working-directory: ./other/
    steps:
    - uses: actions/checkout@v4
    - name: Install fly.io
      run: |
        curl -L https://fly.io/install.sh | sh
        /home/runner/.fly/bin/flyctl version
    - name: fly.io login
      run: |
        /home/runner/.fly/bin/flyctl auth login --email ${{ inputs.login_email }} --password ${{ inputs.login_password }}
        /home/runner/.fly/bin/flyctl auth whoami
    - name: fly.io deploy
      run: |
        /home/runner/.fly/bin/flyctl deploy
        /home/runner/.fly/bin/flyctl scale count 1 --yes
        /home/runner/.fly/bin/flyctl status
        
